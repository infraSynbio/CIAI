# C# temperature driver

This simulated driver demonstrates annotations, typed Function parameters,
progress/cancellation, Get/Set/Operation/EnterExit, and a robot-accessible Nest.

```powershell
dotnet run --project Ciai.Example.Temperature.csproj
```

Then open `http://127.0.0.1:18080/Info`.
