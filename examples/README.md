# Examples

- `csharp-temperature`: runnable .NET 8 simulated driver on port 18080.
- `java-temperature`: runnable Java simulated driver on port 18081.

The examples intentionally contain no vendor SDK, certificate, credential, or
real instrument command. Replace the simulated logic with commands taken from
the device vendor's documentation, and keep deployment secrets outside Git.
